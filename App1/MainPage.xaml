<Page
    x:Class="App1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:w="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock FontSize="18" TextAlignment="Center">
                <Run>Depth Frames Sent </Run>
                <Run Text="{x:Bind DepthFrameCount,Mode=OneWay}"/>
            </TextBlock>
            <TextBlock FontSize="18" TextAlignment="Center">
                <Run>IR Frames Sent </Run>
                <Run Text="{x:Bind IRFrameCount,Mode=OneWay}"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</Page>
